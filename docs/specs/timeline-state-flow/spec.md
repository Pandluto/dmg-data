# 排轴状态来源与时序 Spec

## Status

**Status:** Accepted for implementation

**Updated:** 2026-09-06

**Baseline:** 6b75ec5（2026-09-06）；本规格内容提交为包含本文件的提交。

**Scope:** 桌面普通视图与阅读视图；替代阅读卡片内「按来源动作只显示最终状态」的旧规则。

## Problem Statement

状态的效果来源、触发动作、生效时刻是三个不同事实。旧投影直接使用施放 ID，把战技 Buff 在后续普攻触发的寒冷归给普攻；普通模式又将圆标限定在来源按钮的起手框内，阅读模式则将后续状态压回起手卡片，造成归属和时序双重误读。

当前九动作轴中，别礼 F50 战技施加自身 Buff，普攻命中后在 F137 触发寒冷 2→3。因果链为 status-event:245 → 243 → 53，53 是战技 Buff。F137 必须保留，效果来源为战技、触发为普攻；不能把整个普攻的伤害结算身份改成战技。

## Solution

技能框只表示动作及其名称；状态圆标作为独立投影按真实生效帧排列。来源用于语义归属和详情，不能取代时间坐标。状态持续线延伸到同一状态的下一次变化或到期/消费，不能把不同来源给同一个敌人叠出的寒冷池画成多个独立存量。

拥挤时优先在局部上下分层；同帧事务可聚合。不能为寻找空白而回搬到早先的技能按钮，也不能把不同来源的变化悄悄合并成另一技能的角标。无法展开的密集变化必须明确呈现为时间范围内的多条记录并保留逐条详情。

## User Stories

1. 用户能看出战技赋予的效果在后续普攻触发，并在实际生效位置看到附着增加。
2. 用户打开阅读模式，仍能沿排轴读出 1→2→3→4→消费的顺序，技能名称与状态分别显示。
3. 用户查看状态详情，可区分效果来源、触发动作与帧数；未知来源保持未知，不靠最近的技能猜测。

## Requirements

- 来源追溯只使用显式因果 ID，区分消费者与被消费状态的来源；处理缺失链与循环，不使用角色名特判。
- 普通与阅读视图共用状态事件、真实帧及同帧事务顺序；切换模式不改输入、命中、状态结算或伤害。
- 阅读技能卡不再包含最终附着摘要；保留技能矩形、名称、角色、分组、技力与能量。
- 保留已有 20px 图标、双黑框、窄白环、白色图案阴影及黑色粗体层数。表示消费的 × 保留。
- 持续线按 scope/buff 的共同状态池确定下一变化，不按来源动作截成虚假的独立状态。未观测到自然终止时，仅显示至当前已计算范围，不能凭图示宣称永久持续。
- 状态数据属性暴露事件键、帧、来源命令与触发命令，便于 RIA/DOM 联合检查。
- 批量选择仅针对技能卡，独立状态不能成为可删除的操作。

## Implementation Decisions

- `akeRuntimeLedger` 构建来源/触发分离的只读状态事件；原始运行结果不改写。
- `TimelineStateMarkers` 与画布时间投影负责两种模式的独立状态层；技能按钮负责自己的矩形和名称。
- 状态布局以事件时间坐标为约束，移除将晚生效事件锁回来源 80px 起手框的行为。

## Testing Decisions

冻结当前 Chrome 工作区与运行结果，使用真实 F137 因果链核对效果来源和触发；完整应用隔离 Chrome 比对两种模式的事件帧、位置、独立 DOM 和持续终点。保留必要的通用因果链及布局回归，不改写用户轴。

## Out of Scope

重排用户输入、改角色伤害机制、手机版、改图标配色、推送与部署。

## Further Notes

运行证据：case `browser-case-20260906T120039199z-7b73b10a`，run `run-b4628211-d721-4919-8a9e-46296caad6eb`；九动作总伤 647779.4031308988。原始报告的 parentEventId 已保留因果关系，展示层之前只读取 rootCastId，不能把该展示错误当成伤害数值错误。
