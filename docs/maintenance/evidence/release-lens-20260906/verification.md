# 局部接续放大镜验证记录

- 分析基线：`41af727`，提交日期 2026-09-05 22:12:20 +08:00；验证包含此前尚未提交的工作台与引擎开发。
- 本轮验证日期：2026-09-06；实现文件指纹见 [manifest](implementation-manifest.json)。本记录的内容提交由 `git log -1 -- docs/maintenance/evidence/release-lens-20260906/verification.md` 查询。
- 合同：[Spec](../../../specs/timeline-release-lens/spec.md)。现有 Chrome、原端口 43821、原“洛茜·卡缪排轴”；没有新建演示页或覆盖实验存档节点。

## 可见前端与保存输入

[原生点击/键盘证据](browser-evidence.json)：从洛茜终结旁的接续标记打开镜面，原关系是接卡缪连携第 3 击 F403 后 6 帧。左右方向键每次一帧；改为 3 帧时显示 +0.1 秒、F406。取消后保存输入与 Run 均未变；应用后只记录一次 `ReleaseLensCommitted` 和一次 `AkeCalculationRequested`。前七个动作的实际起止均未变，仅终结从 F409 移到 F406。

旧精准输入 F119 回显为精准窗口 [F114,F126)，以窗口开始后 5 帧表达原时刻。增加到 12 帧得到 F126，界面显示越界，应用按钮禁用；取消后旧存档没有被重新写成普通窗。命中与可接续点分别显示为刻线和带标签菱形，来源名与具体施放帧可识别。

[完整拖动处理链](gesture-evidence.json)：RIA `timeline-drag` 在当前可见 Chrome 派发合成 DOM 鼠标事件，先长按 220 ms，再移到来源停留 260 ms，进入镜面选中 3 帧并停留 220 ms。步骤事件证实镜面在拖动态打开，来源与镜面矩形未变，草稿 F409→F406；取消时输入及 Run 不变，释放时只有一次提交/一次计算。

原生控制器的瞬时 from→to 拖动不足 200 ms，未触发既有长按。因此完整拖动证据明确标为 synthetic，不冒充原生连续手势。原生点击、键盘、窗口边界、覆盖面和回显在 Chrome 检查；原生长按/滚轮的连续手感仍需用户试用反馈。

验证后恢复第 3 击后 6 帧，并点击 Chrome“重新加载”。重开后 staffLines 完全一致，终结仍 F409，期望总伤 118173.725373。此操作验证工作区自动持久化与重读，没有另建/覆盖命名存档节点。

## 运行时语义

[版本/逐命令对照](timing-audit.json) 和 [五组单变量实验](timing-ablation.json) 分开记录旧/新 Run。当前引擎重放旧 fixture，8 条命令及全部 79 次 HP 命中逐项复现。加入真实动作结束依赖后，卡缪 Q 从实际 E1 结束 F119 起手；其后 B 等 Q 自然结束 F270。旧 B 在 F262 的 AllowNext 窗口提前打断 Q。这是保存关系开始生效，不能描述成“显示展开改变时序”，也不能据此认定等待自然结束最优。

固定回归还覆盖：实际命中 + 延迟跟随来源前移/后移、零帧的来源先于后继、汤汤真正结束后同帧切人/下落、过早下落拒绝、精准窗右端排除、普通窗不漂到精准窗、来源失败/循环不制造释放、整套普攻只在末段结束。

## 自动检查

以下命令通过（前端命令在 `demo/lts-ui` 执行）：

- `npm run typecheck`
- `npm run demo:build`（仍有既有大 chunk 提示）
- `node scripts/run-ts-test.mjs /src/components/CanvasBoard/hooks/releaseLensModel.test.ts /src/core/domain/releaseAnchorGraph.test.ts /src/integrations/ake/akeProvider.test.ts`
- `node --test test/ake-release-dependencies.test.mjs test/ake-controller-state.test.mjs test/ake-plunging-impact-input.test.mjs`：16 个案例通过。
- `node --test test/ria-live-debug.test.mjs`：2 个案例通过。

模型回归验证密集映射的整数帧往返、同帧不同接点、视口边缘约束、旧窗口正确回显、窗口右端排除及草稿不修改候选。代码审查修复了后台结果更新后鼠标基准失效、偏移后漏查同帧占用、无效镜面松手误落背景点的问题。后台结算正好在真实拖动中完成的竞态未单独诱发；实现会冻结画布报告与候选、保留输入版本校验，该边界不能写成已完成实机压力验证。

## 验收覆盖与边界

| 验收项 | 证据与状态 |
| --- | --- |
| AC-01–04 | 原生点击/键盘 + 可见 Chrome DOM 拖动复现；三个入口一页，密集点可分页；边缘几何有回归。连续原生手感待试用 |
| AC-05–08、AC-15–16 | 三帧/0.1 秒回显、精准窗边界、取消不变、单次提交/计算及保存关系回显均有结构化证据 |
| AC-09 | 候选/画布冻结、版本检查与监听恢复已实现并审查；结算恰好完成的实机竞态待专项复现 |
| AC-10–11、AC-17–18 | 运行时事件依赖、旧/新时钟单变量及窗口偏移回归 |
| AC-12–13 | 当前 Chrome 持久化重读与既有关系图循环/删除保护回归；未新增或覆盖命名节点 |
| AC-14 | 复用已有截段、控制权、下落/闪避规则；本轮没有穷举所有普攻截段和闪避组合的真实拖动 |

保留卡缪连携门槛“待核验”，已有伤害结算不等于真实游戏触发条件已证实。未新增所有 AllowNext 语义端口、DOT 接点、完整效果轨道、手机交互、增量缓存或全局同帧排序迁移。输入窗口 v1 跟随来源起点+保存偏移，窗口内部时序变化后会核验/拒绝，不会自动寻找新的窗口位置。
