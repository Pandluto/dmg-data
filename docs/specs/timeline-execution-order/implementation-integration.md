# 2026-09-12 本地预览集成

基线：`11ef693`，日期 2026-09-12；最新业务集成提交：`2a0ae61`。本记录另行提交。

## 分支与内容

用户要求的本地分支为 `codex/ui-0912`，工作树位于 `/Users/sailstellar/.codex/worktrees/4c1f/dmg-timeline-order-integration`。原主库仍在 main，业务文件与现有服务保持原样；未推送。

| 来源 | 来源提交 | 集成提交 |
| --- | --- | --- |
| 运行时版本化顺序 | `f304315` | `b4a82f8` |
| 运行时追溯字段修正 | `b9f0bca` | `6e65c9d` |
| A 与主任务：保存、完整队伍上下文、读取恢复 | `05e2a62` | `b71e537` |
| A：逻辑序列、来源事件调度、预演、请求与摘要 | `3617580` | `2a0ae61` |

两个实现记录及来源反例同步接入：[前端](implementation-frontend.md)、[运行时](implementation-runtime.md)。这两个代码包必须一起使用，新前端不会回退发送像素顺序。

主任务补齐读取恢复的 StrictMode 行为：使用恢复快照身份识别纯读取，不使用第一次执行后就清除的布尔标记。读取/替换和连续编辑同步更新内存引用。保存沿用逻辑 nodeIndex；AKE 计算不再从展示快照的按钮位置修复业务输入。目录派生的图标/名称只更新恢复中的内存副本，等正常编辑或保存再落库。正常 WebBootstrap 在目录安装完成后才挂载工作台，且 checkout 先用完整队伍验证迁移，再改工作副本。

## 已检查

- 合并后 `npm --prefix demo/lts-ui run typecheck`、`npm --prefix demo/lts-ui run demo:build` 通过。构建仍有既有大 chunk 提示，未为本轮改动扩大拆包范围。
- 主任务用不监听端口的 SSR 执行实际 `runAkeTeamCalculation`，将 fetch 在内存接至本分支的 `simulateSquadDemo`。切人 X、来源战技 S、依赖 S 的低序号普攻 C：前端 C 起手主控正确，请求三项联合序号唯一、不含 timelineOrder，后端接受并成功执行 C / S。
- 同一输入只改显示 x/y 和更新时间，有效摘要不变；旧输入内存迁移保留原 JSON、原时间戳和全部操作身份。该对照使用合成 UI 输入，不冒充历史真实存档。
- 两任务的定向检查及两条原运行时 fixture 全事实对照见各自记录；主任务核对代码与共享合同，未重复无关全套测试。文档链接和 diff 检查通过。

## 保留的边界

结算报告尚未提供完整的有序来源/切人事件流。当实际结算改变预演的起止、成功或完成状态时，旧控制流会被清除，相关吸附显示“主控事件顺序尚未核验”，不能声称技能非法。完整的结算后因果投影未验收。

已有动作改接续若使后继的观察轨迹无法重放，草稿查询可能返回未知。真实旧 UI 存档迁移、浏览器交互与视觉效果尚未验证；原 API fixture 不能替代缺失的原始 TimelineData。AC 不整体标为完成。

## 本地启动

用户追加授权后，在上述工作树独立启动 `http://127.0.0.1:43822/`，日志 `/tmp/dmg-ui-0912-43822.log`，RIA 档案使用本树 `artifacts/ria-ui-0912`。端口与浏览器存储源独立，旧页工作副本不会自动迁入；未启动或重启浏览器，未停止原服务。

启动后仅以 HTTP 读取检查 `/`、`/api/health`、`/api/ake/catalog`、`/data/default-local-data.json`，全部返回 200。未将 HTTP 可达性当作浏览器交互验收。
