# 长轴重复占位与按操作分配宽度

分析基线：`b041e54`（2026-09-06 15:12:46 +08:00）；记录日期：2026-09-06。
状态：主代理设计、GPT 5.6 Luna max 实现完成；原 Chrome/RIA 长轴核验通过。
本次内容提交与日期通过本文件的 `git log -1 --format='%h %cI'` 查询。

## 当前真实输入

Chrome 已打开“别礼·汤汤｜零潜热启动·四层寒冷爆发”，work-node `ai-timeline-node-81846f75-4459-4b94-ac3d-0c15ab319424`。九个技能加切人，当前占宽 928px，伤害 511169.30090090353。不是用上一轮三技能样本代替长轴。

| 角色 | 操作与实际帧 |
| --- | --- |
| 别礼 | A 0—143；瞬时 B 50—50；E 143—315 |
| 汤汤 | B 0—50；E 60—101；B 103—170；B 171—221；Q 221—306；落地 306—321 |
| 控制 | 别礼切至汤汤 F305 |

## 为什么上一版压缩不够

第一版只压“无起手、无控制”的时段。F50 的瞬时技能已占 80px，F50—60 因同帧起手又占 80px；F305 切人已占 80px，F305—306 因控制标记又占 80px。跨角色的起止点还会把一项操作切成多个各自保宽的部分。当前十四个时段中十一段占 80px，只有三段压成 16px。

## 主代理拟定的算法

1. 保留原有事件边界、零时长列和 materialize 后的真实前后归属。正时长活动段以 16px 为基础；零时长操作和封组等待仍保留完整操作位。
2. 每个技能、普通等待或切人整体跨度至少 80px；多个内部时段共享这份空间。同 lane 相邻不同起手点至少相隔 80px。不能把瞬时控制后的时间段自动当成第二个操作。
3. 将跨度和起手间距写成向前的边界约束，依次求 `x[j] = max(x[j-1] + 基础段宽, x[i] + 80)`。索引基于已 materialize 的起止边界，避免把零时长动作的左右边合并。无自环、无时间推进、无逐像素或逐帧搜索。
4. 对照同一份旧→新边界映射，统一更新列、动作、等待、分组、控制和总宽。默认未启用压缩的旧域调用保持兼容。
5. 分页由真实操作起点和完整 80px 占位判断。操作首段可能只有 16px，不能再仅检查列宽是否达到 80px。沿用前段吸收页末余量的统一重映射。

对当前真实模型做几何约束推算：各段为 80/80/16/80/16/16/64/16/80/16/80/16/16/64，共 640px，比 928px 少约31%。这是实现前推算，后续需由真实 Chrome 核对。上一轮三技能目标仍为 192px。

## 验收与职责

Luna 只改域布局及必要的前端消费，不改战斗引擎、存档、配装或圆标。主代理负责 Spec、原 Chrome 和最终提交。回归限定在真实长轴、原短轴、普通等待跨段、零时长控制、同行间距和窄首段分页。图标占位不得缩小，状态与技能帧不得因显示求解改变。

特别关注预演→结算以及拖拽冻结的共同坐标；本次压缩正确不能自动证明所有连续原生拖动抖动都已解决。

## 主代理复核记录

实现接入原 Chrome 后，RIA 已读到 640px，宽度序列与上面的推算一致；executionDigest、所有 commandId 与实际起止帧、配装均保持原样，伤害仍为 511169.30090090353。当前保存节点已保留完整十个输入，未通过替换技能或删减长轴来获得压缩。

检查要求还包括 Lastrite E 首段仅16px且起点位于320px小页末尾时，整个80px按钮移至下一页；普通wait F0—60与另一行技能F20—100共享时间段，目标为16/64/16共96px，每项操作仍有80px跨度。跨页绘制按真实列与页面的交集裁剪，能量与时间标签取裁剪后边界对应帧；这一显示检查不新增运行时事件边界。

## 最终验证

- 真实原输入：十四个时段仍保留，928→640px，占宽从11.6格变为8格，减少31.03%。全部九个技能的ID与起止帧、切人F305、配装和执行输入摘要均一致，总伤害511169.30090090353未变。该案例本轮无需重建或恢复技能。
- 修复前 Run：`run-af08120f-95f5-47f6-9136-bece341c8fd0`。修复后 Run：`run-c10f908d-5b1c-4612-aed9-b35cf7abdc45`，case `browser-case-20260906T080903055z-ddb15e6a`。
- 队尾交互：RIA 合成拖动汤汤 B 到落地动作 `e8aaq4k8x` 附近，命令 `command-20260906T081226313z-ae3c363c` 打开放大镜，5个候选，当前F315、reason=null；随后取消，完整轴保持原样。此前拖至Q附近的尝试没有打开镜面，未把该观察认定为新的机制错误，也不以队尾结果宣称所有来源都已验收。
- 长轴640、旧短轴192、跨段wait96、窄首段分页与显示裁剪，以及相关时间轴/控制/时序/AKE投影回归通过；TypeScript、diff检查通过。没有重跑无关战斗引擎套件。当前原生截图用于核对长轴可读性，合成拖动不等同于所有连续手动拖动抖动已验证。

## 本轮分工记录

用户明确选择GPT5.6 Luna max。主代理给出前向宽度约束、零时长边界归属、统一重映射和分页准则，并独占原Chrome。Luna完成域模型、画布页内裁剪及定向回归；主代理复核固定长轴和补充两个最小边界用例后完成现场验收。委派至交付约22分钟，token用量未知；模型没有自行改战斗机制或操作用户存档。
