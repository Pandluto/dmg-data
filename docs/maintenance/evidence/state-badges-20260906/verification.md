# 圆形状态徽标与空白避让

分析基线：`a3d9535088da8aada3995aacb6cba3f38e82ce16`（提交日期：2026-09-06T11:22:44+08:00）。
验证日期：2026-09-06。
本次内容提交：包含本文件的提交，可用 `git log -1 --format=%H -- docs/maintenance/evidence/state-badges-20260906/verification.md` 解析。

## 本次修订

根据用户新反馈，替代前一版的 `1→2` 横条。主轴使用 24px 半透明圆形状态徽标，火焰为红色，破防为物理状态色；右下角只显示结果层数。归零变为灰色虚线徽标和“×”。图形复用原状态资源轮廓着色，没有另造游戏状态。

同一施放、同帧、同状态且前后衔接的连续叠层可以共用结果徽标。F148 的破防 1→2、2→3 在主轴显示“3”，两条原始事件仍可悬停查看和点击展开。火附着消费不会并入破防；消费/重新施加不能相互抵消。

避让读取技能图标、技能种类、起手说明、动作刻线与时间字、连携窗口、命中和说明的实际 DOM 边界，按这些边缘寻找空白，在角色技能区域内选择较短位移。首选位置放不下时可上下偏移；尺寸与内容变化会触发重新测量。引线仍连接原投影坐标。没有放大格子或改写战斗输入。

## 原 Chrome 核对

继续使用用户原来的 `127.0.0.1:43821` Chrome，90% 缩放，洛茜/卡缪 8 按钮现场。

- 10 个圆形徽标代表全部 11 条原始状态事件；主轴可见文字不再包含箭头。
- RIA 记录徽标及右下角角标边界，和当前页面的 137 个障碍元素逐一比较，徽标之间及徽标与障碍物之间均无重叠。
- 原生点击破防“3”，弹层出现 F148 的两条叠层记录；Escape 关闭，焦点返回原徽标。最后保留已关闭弹层的主轴。
- 执行摘要、配装、逐事件事实和按钮输入均保持一致。总伤前后为 `118173.72537302248`。
- `timelineData.updatedAt` 因热更新恢复而变化，不能宣称整个序列化存档逐字节一致；除此字段外该快照没有变化。未覆盖命名存档。

结构化证据见 [browser-evidence.json](browser-evidence.json)。完整工作区和调试快照位于本机 `/tmp/ake-state-badges-20260906`、`/tmp/ake-release-lens-20260905/state-badges-*.json`，这些临时路径不作为跨机器永久证据。

## 检查与边界

前端类型检查、`npm run demo:build`、`stateMarkerLayout.test.ts` 均通过。构建仍有已有的 chunk 大小提示。定向回归覆盖现场邻近锚点、上下空白、页边界、100 个事件、完全占满后的折叠入口，以及不能合并消费/重新施加、跨帧、跨施放的事件。

当前原轴足够核对图标外观、两条聚合记录和碰撞几何；未构造极密的原生操作场景。引线采用简短折线，未实现绕开所有文字与其他引线的全局布线路径。卡缪连携的已有“待核验”状态不属于此次显示修订。

本地提交范围为独立布局算法、对应定向回归、Spec 与本证据。`CanvasArea`、状态徽标 React/CSS 和 RIA 接入与此前尚未提交的运行时及界面适配一起保留在原工作区；本提交不冒充完整可独立检出的界面发行版本，没有打包提交既有引擎大改。


## 后续样式修订：圆半径缩小 50%

基线 `c88a4db`（2026-09-06）。按用户随后要求，圆的直径从 24px 改为 12px，底色和内阴影全部移除，改为 0.5px 黑色圆框，保留对应颜色图案；图案、角标及引线同步缩小。Spec 已同步。

原 Chrome 已确认新外观；RIA 测得当前 10 个徽标直径均为 12px，角标计入后与其他元素仍无重叠。此次前后执行摘要、11 条状态事件及总伤 `118173.72537302248` 一致。现有布局定向回归通过；此次纯样式和尺寸修订未扩展运行无关机制测试。

本次追加提交包括样式文件、布局尺寸及现有回归适配、规格与本记录。完整 React/RIA 接入仍随原有开发改动保留在工作区。

后续尺寸微调（基线 `40be302`，2026-09-06）：用户要求在 12px 基础上放大 25%，最终直径为 15px，图案和角标同步放大。原 Chrome 与 RIA 已确认 15px、无徽标碰撞、执行摘要不变，现有布局回归通过。

最终指定尺寸（基线 `087bf0a`，2026-09-06）：用户指定圆标直径 20px，图案及角标同比调整，透明底与黑色细线框保持。原 Chrome 已核对；RIA 测得 20px，徽标及角标无碰撞，执行摘要不变；现有布局回归通过。
