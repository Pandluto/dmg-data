# 寒冷已施加但汤汤连携没有合法落点

分析基线：`4344816`（2026-09-06 12:06:46 +08:00）与现有未提交工作树。
核验日期：2026-09-06。本次修复尚未提交；不能把分析基线当作修复提交。

## 观察与第一处分歧

当前 Chrome 的两个输入是别礼 A@0、汤汤 B@0。RIA Run `run-af52db31-e299-46eb-931a-81f436164031`，case `browser-case-20260906T060136505z-5d4311d2`，寒冷事件 `status:28` 在 F23 施加一层。

引擎实际产生 `combo-window:1`，规则 `tangtang.enemy-receives-cold`，区间为 [23,202)。整轴结束时记录为 expired/TIMEOUT，但该历史区间仍允许插入新连携。源命令 ID 为 null，保留未知来源，不按同帧猜测。

第一处分歧在 `projectSettledAkeTimeline`：匹配当前输入的报告只覆盖了动作、命中坐标与技力，遗漏 comboWindows。轻量预演未预测子伤害触发的窗口，画布于是拿空数组过滤掉汤汤 E 的全部吸附点。这个反例说明前端准入投影不完整，不能据此认定引擎未施冷或汤汤需要更高附着层数。

## 修复与范围

匹配报告的 comboWindows 覆盖预演窗口，包括显式空数组；仅旧报告缺字段时保留预演。active 映射为 ready，历史 expired 保留有效区间，已消费和 suppressed 仍被阻止。保留未知来源为 null，无来源的窗口不制造来源相对的精准接点；已有运行时精准输入适配继续承担该入口。

新增回归从实际分歧进入现有拖放门槛：修改前 F50 错误拒绝，修改后 F23/F50 接受、F22/F202 拒绝；另覆盖已消费、抑制、显式清空、旧报告兼容及不改写输入。

原 Chrome 通过 RIA 合成拖动汤汤 E 到 B 附近再取消：命令 `command-20260906T061550427z-083d6ec7` 返回 releaseLensOpened=true，两个原输入保留。此核验只证明原页面的拖动处理与窗口接通，没有追加技能或宣称验证了原生手感。

即时投影回归、吸附策略回归和 TypeScript 检查通过。吸附策略旧夹具漏了 commands 字段，本次补为空数组以符合已有查询合同。

同轮圆标样式按用户要求交由 GPT 5.6 Luna max 处理：固定 CSS 范围复用技能球外观，约 4 分钟，token 用量不可用；未对这项样式工作运行额外验证或测试。

## 避免再漏判

遇到“状态已出现但操作不合法”，沿同一输入依次对照运行时事件、报告窗口、画布投影、吸附过滤结果。只验证引擎可释放或只看状态图片，都不能证明用户的入口可以操作。
