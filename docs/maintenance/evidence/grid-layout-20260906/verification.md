# 格子 1.5 倍与信息分区验证

- 分析基线：`280a238`，提交日期 2026-09-06（Asia/Shanghai）。
- 本次内容版本：提交本文件的 commit；实现尚在工作区，含前轮未提交的接续放大镜依赖，不将文档提交号冒称实现版本。
- 规格：[GRID-01](../../../specs/timeline-release-lens/spec.md#requirement-grid-01-放大操作格并分配信息区域)。
- 证据：[原 Chrome / RIA 对照](browser-evidence.json)。

## 已验证

1. 原可见 Chrome，端口 43821、桌面视口 1633×842、浏览器缩放 90%。基础格从 80×30 改为 120×45，RIA DOM bounds 测得完整按钮操作区从 80×60 改为 120×90，图标仍 24px。
2. 用户本轮开始时正在编辑的是别礼、汤汤三动作轴。恢复后执行摘要与输入 executionDigest、武器装备配置一致：释放帧 F0、F0、F111，35 次伤害命中，伤害 96655.45987098664，尾伤至 F222。
3. 用 RIA 在同一可见 Chrome 合成 DOM 鼠标长按及移动：移动别礼战技靠近汤汤普攻，镜面成功打开，再取消。前后 timelineData 完全相同、runId 未变、镜面关闭。该证据不能替代连续原生拖动的手感验收。
4. 首次尝试拖动汤汤战技被已有普攻尾链锁定规则拒绝；没有因此解锁或修改原普攻五段及接续关系。随后选择可移动的别礼战技完成验证。
5. 原生点击 F134 寒冷附着 1→2 标记，RIA click 的实际 target 为该状态按钮。投影的层叠上下文已调整，状态按钮没有被扩大的透明技能区域吞掉。
6. 类型检查、前端构建、已有 gridSnapLayout / releaseLensModel / sharedVariableRateTimeline 范围回归通过。构建保留原有大 chunk 提示。

## 现场恢复与边界

刷新以及 TS 热更新触发整页重载时，页面会回到正式存档的旧十动作轴，未保留刷新前的当前编辑态。本轮未把旧存档当成新结果：先有 RIA 输入备份，再经原 Chrome 开发者控制台调用既有工作区存储与 openView 命令恢复三动作，最后重新对照。没有新建或覆盖命名存档。正式刷新恢复链仍待单独修复；也观察到 latest-report.v3 的 sessionStorage 配额告警，但没有据此认定它就是编辑态被覆盖的根因。

保留每组四个角色槽及 14 个逻辑节点；长轴在画布内横向滚动。空行折叠、任意密集相邻帧标签避让、横向滚动到页边后的完整原生拖动验收未纳入本次完成范围。
